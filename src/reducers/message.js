var data = [
	{
		username: 'Nino',
		message: 'Hello world...'
	}
]

const messages = (state = data, action) => {
	switch (action.type) {
	    default:
	      return state
  	}
}

export default messages